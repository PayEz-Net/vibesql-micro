// vibesql-micro
// PostgreSQL + JSONB + HTTP API in one command.
// Use `npx vibesql-micro serve` or install globally with `npm install -g vibesql-micro`.
// Docs: https://vibesql.online/docs.html
module.exports = {};
